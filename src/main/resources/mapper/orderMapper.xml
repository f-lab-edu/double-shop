<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.doubleshop.domain.order.mapper.OrderMapper">
    <insert id="insertOrder" parameterType="Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ORDER (member_id, ordered_at, order_status, total_price, status, status_update_time)
        VALUES (#{memberId}, #{orderDate}, #{orderStatus}, #{totalPrice}, #{status}, #{statusUpdateTime})
    </insert>
    <select id="selectByOrderId" parameterType="long" resultType="Order">
        SELECT id, member_id, ordered_at, order_status, total_price, status, status_update_time
        FROM ORDER WHERE id = #{id}
    </select>
    <select id="selectAllOrders" resultType="Order" parameterType="Pageable">
        SELECT id, member_id, ordered_at, order_status, total_price, status, status_update_time
        FROM ORDER ORDER BY id DESC LIMIT #{size} OFFSET #{page}
    </select>
    <update id="updateOrder" parameterType="Order">
        UPDATE ORDER SET
                            member_id = #{memberId},
                            ordered_at = #{orderDate},
                            order_status = #{orderStatus},
                            total_price = #{totalPrice},
                            status = #{status},
                            status_update_time = #{statusUpdateTime}
        WHERE id = #{id}
    </update>
    <update id="updateOrderStatus" parameterType="StatusRequest">
        UPDATE ORDER SET status = #{status} WHERE id = #{id}
    </update>
    <delete id="deleteOrder" parameterType="Status">
        DELETE FROM ORDER WHERE status = #{TO_BE_DELETED}
    </delete>
</mapper>